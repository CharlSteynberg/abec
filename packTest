/*****************************************\

    THIS IS EXPECTED TO RUN IN NODE.JS
    run: > node ./packTest

\*****************************************/



require(`./abec.min.js`);

(function testMain()
{
    // run some tests .. if somthing horrible went wrong in abec, the test should have failed already
    // TODO :: lots of tests .. on many interfaces .. some async

    process.exit(0);
})();
